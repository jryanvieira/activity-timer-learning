Em testes, para entender como o código irá rodar em produção, podemos instalar o package http-server;
Para instalar: `npm i -g http-server`

Para rodar, entrar na pasta dist e rodar o comando: `http-server`